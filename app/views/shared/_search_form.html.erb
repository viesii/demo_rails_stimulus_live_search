<%= form_with url: player_search_index_path, method: :get, data: { action: "ajax:success->search#handleResults", target: "search.form" } do |form| %>
  <div>
    <%= form.text_field :search, data: { action: "input->search#search" }, autocomplete: "off" %>
  </div>
<% end %>